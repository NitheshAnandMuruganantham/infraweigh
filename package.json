{
  "name": "infraweigh",
  "version": "1.0.0",
  "license": "MIT",
  "private": "true",
  "dependencies": {
    "concurrently": "^7.2.1",
    "firebase": "^9.8.3",
    "firebase-tools": "^11.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/mentorSqr/infraweigh.git",
  "author": "nithesh  anand <anand252825@gmail.com>",
  "scripts": {
    "prepare": "husky install",
    "dev": "concurrently \"yarn dev:terminal\" \"cd packages/serverless && firebase emulators:start\" \"cd packages/serverless && yarn build:watch\" \"cd packages/hasura && hasura console\" \"cd packages/local-polyfils && node . \"",
    "build": "cd packages/terminal && yarn build && mv dist public && cp -R public ../serverless",
    "dev:terminal": "yarn workspace terminal dev"
  },
  "devDependencies": {
    "husky": "^8.0.1"
  }
}

{
  "name": "infraweigh",
  "version": "1.0.0",
  "license": "MIT",
  "private": "true",
  "dependencies": {
    "concurrently": "^7.2.1",
    "firebase": "^9.8.3",
    "firebase-tools": "^11.0.1",
    "razorpay": "^2.8.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/mentorSqr/infraweigh.git",
  "author": "nithesh  anand <anand252825@gmail.com>",
  "scripts": {
    "dev": "concurrently \"cd packages/serverless && firebase emulators:start\" \"cd packages/serverless && yarn build:watch\" \"cd packages/hasura && hasura console\" \"cd packages/local-polyfils && node . \"",
    "build": "cd packages/source && yarn nx run terminal:build:production && mv dist/packages/terminal dist/packages/public && cp -R dist/packages/public ../serverless"
  }
}

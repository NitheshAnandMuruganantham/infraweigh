# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools
name: Deploy to Firebase Staging Hosting on merge
"on":
  push:
    branches:
      - staging
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      NX_BASE_FIREBASE: ${{secrets.NX_BASE_FIREBASE_STAGING}}
      NX_BASE_URL: ${{secrets.NX_BASE_URL_STAGING}}
      NX_BASE_WS_URL: ${{secrets.NX_BASE_WS_URL_STAGING}}
      NX_ENV: staging
    steps:
      - uses: actions/checkout@v2
      - run: npm i -g yarn && cd scripts && yarn build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          entrypoint: firebase
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_INFRA_WEIGH }}"
          channelId: live
          projectId: infra-weigh-staging

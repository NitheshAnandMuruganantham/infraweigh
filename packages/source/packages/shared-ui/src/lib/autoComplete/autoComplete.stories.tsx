import { Button } from '@mui/material';
import AutoComplete from '.';

export default {
  name: 'AutoComplete',
  component: Button,
};

export const AutoCompleteInput = () => <Button>hello</Button>;
